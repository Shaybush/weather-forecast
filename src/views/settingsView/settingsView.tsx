import ThemeSettings from "./components/themeSettings/themeSettings";

const SettingsView = () => {
  return (
    <div className="p-2">
      <ThemeSettings />
    </div>
  );
};

export default SettingsView;
